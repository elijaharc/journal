<div class="card mt-lg-5 shadow-sm rounded">
    <div class="card-body">
        <h1 class='mb-3'>Create New Task</h1>
        <%= render 'form', task: @task %>
        
    </div>
</div>

<%= link_to 'Back', category_path(@category), class: 'd-flex justify-content-center mt-2' %>