<% @tasks.each_with_index do |task, index| %>
    <% if index > 1 %>
        <%= will_paginate @tasks, list_classes: %w(pagination justify-content-center mt-2 btn-sm) %>
    <% end %>
<% end %>
<%= will_paginate @tasks, list_classes: %w(pagination justify-content-center) %>