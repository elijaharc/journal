<!DOCTYPE html>
<html>
  <head>
    <title>Journal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
   
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@100;200;400;500&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.0/font/bootstrap-icons.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <%= render 'layouts/navbar' %>
    <% if params[:action] == 'home' %>
      <%= yield %>
    <% else %>
      <div class="container col-lg-6">
        <%= yield %>
        <p class="notice text-center mt-4 mb-0"><%= notice %></p>
        <p class="notice text-center mt-4 mb-0"><%= alert %></p>
      </div>
      
    <% end %>
    <footer class='fixed-bottom text-center'><small id='footer'>2021 &#169; Elijah Arcedera</small></footer>
  </body>
</html>
